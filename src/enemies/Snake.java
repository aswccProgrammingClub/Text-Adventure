package src.enemies;

import src.models.Enemy;

public class Snake extends Enemy{

    public Snake(int hp) {
        super(hp);
        //TODO Auto-generated constructor stub
        //Should we add abilities to the enemy? ex: snake can have venom bite OR tail whip OR strangulation
        //Each ability could do different damage and can be randomized by using the java.util.Random
    }

}